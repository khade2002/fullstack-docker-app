import React from 'react';
import ReactDOM from 'client';
import './index.css'; // A place for global styles
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
